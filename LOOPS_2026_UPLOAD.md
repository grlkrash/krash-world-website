# Loops 2026 - S3 Upload Checklist

Add these files to `krash-beatstore-aws` S3 bucket under `beats/`:

**Previews (for store playback):**
- `loop-16-preview.mp3` ← use `day three.mp3`
- `loop-17-preview.mp3` ← use `day two loop.mp3`

**Full files (for purchase download):**
- `day-three.wav`, `day-three.mp3` (loop-16)
- `day-two-loop.wav`, `day-two-loop.mp3` (loop-17)
